- Files.list(dirPath) – To najszybszy sposób  na wyciągnięcie zawartości pojedynczego folderu. Nie wchodzi on "głębiej" do podfolderów. 
  - (Files.walk() - przeszukuje cały komputer z podfolderami).


- path.getFileName().toString() – Ścieżka (Path) to cały adres (np. src/_21/secret_spells.txt). Żeby wyciągnąć samą końcówkę (nazwę pliku), używamy metody .getFileName().